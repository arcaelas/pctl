# Installation

```bash
npm install -g @arcaelas/pctl
```

## Requirements

- Node.js 20+
- Docker (for building images)
- AWS CLI (for AWS provider)
- gcloud CLI (for GCP provider)
- SSH access (for remote Docker provider)
